/* Castilla Glass Visualizer — front-end config.
 * Paste your Google Maps **browser** API key below to enable address lookup,
 * Street View and the 3D location view. It's safe to commit when the key is
 * restricted to your site's domain (HTTP referrer) in Google Cloud.
 * Leave blank to keep the photo-upload flow only.
 *
 * Enable these APIs on the key: Maps JavaScript API, Places API,
 * Street View (Static + JS), Map Tiles API (for 3D).
 * The Street View images are fetched server-side — set MAPS_API_KEY in Netlify too.
 */
window.CG_CONFIG = {
  mapsBrowserKey: "",
};
